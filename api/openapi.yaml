openapi: 3.1.0
info:
  title: Diwy Public API Contract
  version: 1.0.0
  description: A reduced illustrative contract; it is not the complete production API.
paths:
  /api/public/capabilities:
    get:
      summary: Return client-safe capabilities
      responses:
        '200':
          description: Available public capabilities
